apiVersion: 1

providers:
  - name: Observe
    orgId: 1
    folder: Observe
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
